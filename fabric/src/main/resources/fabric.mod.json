{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${authors}"
  ],
  "contact": {
  },
  "license": "${license}",
  "icon": "icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "net.electrisoma.testmod.fabric.TestModImpl"
    ],
    "client": [
      "net.electrisoma.testmod.client.fabric.TestModClientImpl"
    ]
  },
  "mixins": [
    "testmod-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": "${minecraft}",
    "flywheel": "${flywheel}",
    "veil": "${veil}",
    "ponder": "${ponder}",
    "visceralib": "${visceralib}"
  }
}