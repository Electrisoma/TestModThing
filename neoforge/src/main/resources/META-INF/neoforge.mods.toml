modLoader="javafml"
loaderVersion="*"
license="${license}"

[[mods]]
modId="${id}"
displayName="${name}"
description="${description}"
authors="${authors}"
version="${version}"
logoFile="icon.png"
logoBlur=false

[[mixins]]
config = "testmod-common.mixins.json"

[[dependencies."${id}"]]
modId="flywheel"
type="required"
versionRange="${flywheel}"
ordering="AFTER"
side="CLIENT"

[[dependencies."${id}"]]
modId="veil"
type="required"
versionRange="${veil}"
ordering="AFTER"
side="CLIENT"

[[dependencies."${id}"]]
modId="ponder"
type="required"
versionRange="${ponder}"
ordering="AFTER"
side="BOTH"

[[dependencies."${id}"]]
modId="visceralib"
type="required"
versionRange="${visceralib}"
ordering="AFTER"
side="BOTH"